import matplotlib.pyplot as plt
import numpy as np


str_prior_mean = "1"
prior_mean = double(str_prior_mean)
str_prior_SD = "1"
prior_SD = double(str_prior_SD)

def gaussian(mean, sd, weight):
    

fig, ax = plt.subplots()
ax.set_title('gaussian_product')
ax.legend(loc = 'upper left')
plt.show()
